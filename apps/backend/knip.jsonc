// Unused-code and dependencies check (`npm run knip`). Plugins and routes are declared as entry points because nothing in src/ imports them: app.ts autoloads them by directory.
{
  "entry": ["src/routes/**/*.ts", "src/plugins/**/*.ts"]
}
